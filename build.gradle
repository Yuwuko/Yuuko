plugins {
    id 'application'
}

apply plugin: 'java'
apply plugin: 'idea'

group = 'com.yuuko.core'
version = '2019-05-15_1'
sourceCompatibility = 1.10
mainClassName = 'com.yuuko.core.Yuuko'

jar {
    doFirst {
        manifest {
            if(!configurations.compile.isEmpty()) {
                attributes(
                        'Class-Path': configurations.compile.collect { it.toURI().toString() }.join(' '),
                        'Main-Class': mainClassName
                )
            }
        }
    }
}

repositories {
    mavenCentral()
    mavenLocal()
    jcenter()
    maven { url = 'https://jitpack.io' }
}

dependencies {
    // Discord API
    compile 'net.dv8tion:JDA:3.8.3_462'
    compile 'com.sedmelluq:lavaplayer:1.3.14'
    compile 'com.sedmelluq:jda-nas:1.0.6'
    compile 'com.github.FredBoat:Lavalink-Client:fbd297512f'
    // Bot Lists
    compile 'com.github.DiscordBotList:DBL-Java-Wrapper:2.0.0'
    compile 'com.github.BasketBandit:ddblAPI.java:0.4.0'
    // External API
    compile 'com.google.apis:google-api-services-youtube:v3-rev211-1.25.0'
    // JSON
    compile 'com.fasterxml.jackson.core:jackson-annotations:2.9.8'
    compile 'com.fasterxml.jackson.core:jackson-databind:2.9.8'
    // Web Clients
    compile 'com.squareup.okhttp3:okhttp:3.14.1'
    compile 'org.jsoup:jsoup:1.11.3'
    // Database
    compile 'mysql:mysql-connector-java:8.0.15'
    compile 'org.apache.commons:commons-dbcp2:2.6.0'
    // Logging
    compile 'org.slf4j:slf4j-simple:1.7.26'
    // Reflection
    compile 'org.reflections:reflections:0.9.11'
}
